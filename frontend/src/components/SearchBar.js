import React from 'react';

const SearchBar = ({ isMobile, setSearchOpen }) => {
  return (
    <div>
      {/* Placeholder for SearchBar component */}
      <input type="text" placeholder="Search legal discussions..." />
    </div>
  );
};

export default SearchBar;
